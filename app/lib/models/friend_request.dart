enum FriendshipStatus {
  active,          // They are already in your friends list
  requestSent,     // You sent them a request (pending)
  requestReceived, // They sent you a request (pending)
  none,            // No relationship
}