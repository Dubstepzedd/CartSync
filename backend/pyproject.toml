[project]
name = "cartsync-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "flask-bcrypt",
    "flask-cors",
    "flask-jwt-extended",
    "flask-sqlalchemy",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
]
